"use strict";
class Employee {
    constructor(id, name, address) {
        this.id = 1;
        this.name = "John";
        this.address = "hiighway 71";
        // return `${name} stays at ${address}` ;
    }
}
